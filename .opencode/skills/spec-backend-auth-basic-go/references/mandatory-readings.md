# Leituras obrigatorias

- `apps/backend/cmd/api/main.go`
- configuracao de auth atual (se houver)
- dominio atual em `modules/auth` (se existir)

## Observacoes

- Confirmar formato do JWT antes de implementar.
